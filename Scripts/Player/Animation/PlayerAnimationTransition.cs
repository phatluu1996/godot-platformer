public class PlayerAnimationTransition
{
    public string fromId;
    public string toId;
    public PlayerAnimationTransition(string from, string to)
    {
        fromId = from;
        toId = to;
    }
}